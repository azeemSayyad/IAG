node_modules/
dist/
build/
.next/
*.pyc
__pycache__/
.env
.env.local
.env.*.local
*.log
.DS_Store
coverage/
.mypy_cache/
.pytest_cache/
alembic/versions/*.pyc

# --- secrets / sensitive (NEVER commit) ---
.env.production
.env.production.local
.env.ngrok
*.pem
*.key
*.p12
infrastructure/certs/
infrastructure/kubernetes/**/secrets.yaml
infrastructure/kubernetes/**/secrets-manager.yaml

# --- runtime artifacts / generated junk ---
.venv/
celerybeat-schedule
celerybeat-schedule-*
.opencode-anchored-summary.md
models/registry/
apps/backend-api/app/ml/artifacts/*.json
# ML model-registry test-suite scenario artifacts (regenerated by tests; not real models)
apps/backend-api/models/registry/test_*/

# Local-only Docker Compose overrides (machine-specific ports/env) — not for production
docker-compose.override.yml
